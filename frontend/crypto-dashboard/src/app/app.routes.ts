import { Routes } from '@angular/router';
import { MarketChartComponent } from './components/market-chart/market-chart';

export const routes: Routes = [
  { path: '', component: MarketChartComponent }
];
